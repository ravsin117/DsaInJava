import java.util.LinkedList;
public class linkedList2 {

    public class Node{
        Node next = null;
        int data = 0;
        Node(){

        }
        Node(int data){
            this.data = data;
        }
    }

    private Node head = null;
    private Node tail = null;
    private int size = 0;

    public int size() {
        return this.size;
    }

    public boolean isEmpty() {
        return this.size == 0;
    }

    // unfold a linkedlist 
    

    // remove kth node from end

    // segregate 012

    // merge k sorted list

    // reverse Node in k group

    // reverse in range

    // add two linkedlist
    
    // subtract two linkedlist
    
    // multiply two linkedlist

    // clone a linkedlist with random pointer

    // cycle detection in a linkedlist

    // starting point of linkedlist

    // intersection pt of 2 Linkedlist

    // finding cycle length 

    // segregate node of linkedlist over last index

    // segreagate over any pivot index

    // quickSort in LL

    // LRU cache implementation


    public static void main(String [] args){

    }
}
